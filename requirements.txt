# Omnipath v3.0 Requirements
# Next-generation multi-agent platform with event-driven architecture

# Web Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.9

# Database
sqlalchemy==2.0.25
psycopg2-binary==2.9.9
asyncpg==0.29.0
aiosqlite==0.20.0
alembic==1.13.1

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-dotenv==1.0.0
pydantic-settings==2.1.0

# Redis (Caching & Streams)
redis==5.0.1
hiredis==2.3.2

# NATS (Event Bus)
nats-py==2.9.0

# Observability
# OpenTelemetry instrumentation 0.48b0 imports pkg_resources, which was
# removed in setuptools 81. Keep the runtime dependency explicitly compatible.
setuptools<81
opentelemetry-api==1.27.0
opentelemetry-sdk==1.27.0
opentelemetry-exporter-otlp==1.27.0
opentelemetry-instrumentation-fastapi==0.48b0
langfuse==2.51.4

# LLM & Agent Frameworks
# LangChain packages with compatible versions
langchain==0.3.17
langchain-core>=0.3.78,<0.4.0
langchain-openai==0.3.35
langchain-anthropic==0.3.7
langchain-google-genai==2.0.8
langchain-ollama==0.2.2
langgraph==0.2.55
# Note: langgraph-prebuilt excluded due to incompatible langchain-core requirement

# Rate Limiting
slowapi==0.1.9

# Monitoring & Metrics
prometheus-client==0.19.0

# Utilities
python-dateutil==2.8.2
pydantic==2.10.5
packaging>=23.2,<25.0

# Logging
python-json-logger>=3.0.0

# HTTP client (async webhook delivery)
aiohttp>=3.9.0

# Password hashing
bcrypt>=4.0.0

# YAML parsing (policy engine)
PyYAML>=6.0

# Task Scheduling
apscheduler==3.11.2

# Reddit API Integration
praw==7.8.1

# Cryptography (API key vault AES-256 encryption)
cryptography>=42.0.0

# Phase 3: Browser Automation & Social Media
playwright>=1.40.0
tweepy>=4.14.0

# Development
pytest==7.4.4
pytest-asyncio==0.23.3
httpx==0.27.2
black==24.1.1
flake8==7.0.0
autoflake>=2.0.0
mypy==1.8.0
pytest-cov==4.1.0
bandit>=1.7.5
safety>=3.0.0

# Specialized Agent Dependencies
email-validator==2.1.0
pyreqwest-impersonate==0.5.3  # compatible prebuilt wheel for Python 3.12
duckduckgo-search==6.1.7  # legacy — kept for compatibility
ddgs>=0.3.0               # successor to duckduckgo-search; used by WebSearchTool
html2text>=2024.2.26      # HTML-to-text; used by WebPageReaderTool
beautifulsoup4>=4.12.0    # HTML parsing; used by WebPageReaderTool
numpy==1.26.4
typer>=0.16.0
fastapi-users==13.0.0
